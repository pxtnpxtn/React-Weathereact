import React from "react";

const Title = () => (
  <React.Fragment>
    <h1>Weathereact</h1>
    <p>Find out the temperature, conditions and more...</p>
  </React.Fragment>
);

export default Title;
